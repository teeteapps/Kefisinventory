@model IEnumerable<DBL.Models.Products>
@{
    ViewData["Title"] = "Productlist";
}

<div class="row">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-body">
                <table class="table table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>Product Name</th>
                            <th>Quantity</th>
                            <th>Price per Product</th>
                            <th>Reorder Level</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var data in Model)
                        {
                            <tr>
                                <td>@data.Productname</td>
                                <td>@data.Quantity</td>
                                <td>@data.Price</td>
                                <td>@data.Reorderlevel</td>
                                <td><a href="@Url.Action("Makeasale","Product",new {id=data.Productid })" class="btn btn-sm btn-info">Sale</a></td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>